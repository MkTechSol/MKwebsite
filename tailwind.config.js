// tailwind.config.js
export default {
  content: ["./index.html", "./src/**/*.{vue,js}"],
  darkMode: 'class', // Enables dark mode via class — we won't use the 'dark' class
  theme: {
    extend: {},
  },
  plugins: [],
}
